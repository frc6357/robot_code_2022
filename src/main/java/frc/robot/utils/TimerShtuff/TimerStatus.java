package frc.robot.utils.TimerShtuff;

/**
 * Enum containing states for a timer.
 */
public enum TimerStatus
{
    /** Timer is running. */
    IN_PROGRESS,
    /** Timer has timed out */
    FINISHED
}
